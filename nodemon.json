{
  "ignore": [
    "!(package*.json|pnpm-*.yaml|yarn.lock)"
  ],
  "execMap": {
    "json": "ni && nodemon --ext 'js,ts' -w functions -x 'tsx' $SERVER_PATH/server.ts"
  },
  "ext": "json,yaml,lock"
}